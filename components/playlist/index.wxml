<!--components/playlist/index.wxml-->

<view class="playlist-container" catch:tap="goToMusicList">
  <image src="{{data.picUrl}}" class="playlist-img"></image>
  <text class="playlist-playcount">{{playCount}}</text>
  <view class="playlist-name">{{data.name}}</view>
</view>