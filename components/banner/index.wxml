<!--components/banner/index.wxml-->
<swiper
  autoplay="{{true}}"
  duration="500"
  circular="{{true}}"
>
  <block wx:for="{{bannerData}}" wx:key="index">
    <swiper-item>
      <image class="index-banner" src="{{item.pic}}" mode="widthFix"></image>
    </swiper-item>
  </block>
</swiper>